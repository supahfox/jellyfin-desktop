[package]
name = "jfn-jellyfin"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "jfn_jellyfin"
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
