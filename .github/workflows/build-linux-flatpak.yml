name: build-linux-flatpak

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
    tags:
      - 'v*'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Check Flatpak manifest matches CEF_VERSION
        run: python3 dev/tools/update_flatpak_manifest.py --check

      - name: Install Flatpak
        run: |
          sudo apt-get update
          sudo apt-get install -y flatpak flatpak-builder

      - name: Add Flathub
        run: |
          flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo

      - name: Install SDK
        run: |
          flatpak install --user -y flathub org.freedesktop.Sdk//25.08 org.freedesktop.Platform//25.08

      - name: Build Flatpak
        run: ./dev/flatpak/build.sh

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: linux-flatpak
          path: dist/jellyfin-desktop.flatpak
