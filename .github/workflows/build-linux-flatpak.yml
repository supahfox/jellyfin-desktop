name: build-linux-flatpak

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Install Flatpak
        run: |
          sudo apt-get update
          sudo apt-get install -y flatpak flatpak-builder

      - name: Add Flathub
        run: |
          flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo

      - name: Install SDK
        run: |
          flatpak install --user -y flathub org.freedesktop.Sdk//25.08 org.freedesktop.Platform//25.08

      - name: Build Flatpak
        working-directory: dev/flatpak
        run: |
          flatpak-builder --user --repo=repo --force-clean build-dir org.jellyfin.JellyfinDesktop.yml

      - name: Create bundle
        working-directory: dev/flatpak
        run: |
          flatpak build-bundle repo jellyfin-desktop.flatpak org.jellyfin.JellyfinDesktop

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: linux-flatpak
          path: dev/flatpak/jellyfin-desktop.flatpak
